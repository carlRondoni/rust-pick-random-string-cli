# Pick Random Sting from a list CLI

Small CLI to pick a random string from a list.

## Requirements

- Rust

## Usage

```bash
cargo run -- <list>
```

Example:

```bash
cargo run -- "foo,bar,baz"
```